/**
 * Mirrors `backend/app/core/limits.py`. The API enforces these; the constants
 * here only keep the form from offering something that will be rejected.
 */
export const CATEGORY_NAME_MAX_CHARS = 50;
export const CATEGORY_COUNT_MAX = 200;
